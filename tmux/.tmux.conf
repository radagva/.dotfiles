set -g default-terminal "xterm-256color"
set -ag terminal-overrides ",xterm-kitty:Tc"

set -g prefix C-a
unbind C-b
bind-key C-a send-prefix
unbind c

bind t display-popup -d "#{pane_current_path}" -w "80%" -E "zsh"
bind C-c display-popup -d "~/.dotfiles" -w "80%" -h "80%" -E "nvim"
bind c new-window -c "#{pane_current_path}"

unbind %
bind | split-window -h -c "#{pane_current_path}"

unbind '"'
bind - split-window -v -c "#{pane_current_path}"

unbind r

bind r source-file ~/.tmux.conf
set -s escape-time 0
set -g repeat-time 180
set -g set-titles on
set -g set-titles-string "#S / #W [#{pane_current_command}]"

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r h resize-pane -L 5
bind -r l resize-pane -R 5
bind -r m resize-pane -Z


set -g mouse on
set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection
unbind -T copy-mode-vi MouseDragEnd1Pane

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'

set -g @vim_navigator_mapping_left "C-Left C-h"  # use C-h and C-Left
set -g @vim_navigator_mapping_right "C-Right C-l"
set -g @vim_navigator_mapping_up "C-k"
set -g @vim_navigator_mapping_down "C-j"
set -g @vim_navigator_mapping_prev ""  # removes the C-\ binding

####
#
# CUSTOM THEME
#
####

# Basic setup
set -g status on
set -g status-interval 1
set -g status-justify left
set -g status-position top

crust="#11111b"          # Background
mantle="#181825"         # Status bar background
base="#1e1e2e"           # Pane background
text="#cdd6f4"           # Main text
subtext0="#a6adc8"       # Secondary text
blue="#89b4fa"           # Active pane border
yellow="#f9e2af"         # Message text
red="#f38ba8"            # Current window highlight
overlay0="#6c7086"       # Inactive window text

# Background & Foreground
bg="$base"
fg="$text"
status_bg="$mantle" 
status_fg="$text"

set -g mode-style bg=#393836

# Pane borders
set -g pane-border-style "fg=#546D9B"
set -g pane-active-border-style "fg=#546D9B"

# Status bar
# set -g status-style "bg=$status_bg,fg=$status_fg"

set -g status-left-length 100
set -g status-left-style "bold"
set -g status-left ""

# Status right (time, date, host)
set -g status-right-length 100
set -g status-right ""

# Window status
set -g window-status-format " #[fg=$subtext0]#I #W "
set -g window-status-current-format " #[fg=$blue,bold] #[fg=$text,bold]#W "
set -g window-status-separator ""

# Message styling
set -g message-style "fg=$yellow,bg=$bg,bold"


run '~/.tmux/plugins/tpm/tpm'

set -g status-style bg=default
